# Optional integration settings (server-side only)
# Demo mode remains default even when these are absent.

ZERVE_ENABLED=false
ZERVE_BASE_URL=https://api.zerve.ai
ZERVE_PROJECT_ID=
ZERVE_API_KEY=
ZERVE_TIMEOUT_SECONDS=4.0

# Optional backend URL for the web app to query backend integration status.
# Leave empty for local fallback status behavior.
MACROFAIR_API_BASE_URL=
